local ContextActionService = game:GetService("ContextActionService")

-- Allows the player to zoom in on Zoomables
local function ZoomableHandler()
    --ContextActionService:BindAction("Zoomable", OnAction, true)
end

return ZoomableHandler